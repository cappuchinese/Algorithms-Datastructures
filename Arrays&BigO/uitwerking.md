# Uitwerking opgave 2

### Opgave 1
```
DetermineMaxElement
Goal: Return the maximum element of an Array
Input: The Array that needs to be checked
Output: Integer of the maximum element
Preconditions: The Array contains only integers
Postconditions: The Array remains unchanged
Algorithm:
    max = -infinity
    for each inem in A:
        if item > max:
            max = item
    return max
```

### Opgave 2
```
AddIntSorted
Goal: Adds a Integer to a sorted Array, in the right place
Input: An Integer to be added to the Array, and the refering Array 
Output: An Array with 
Preconditions: The Array is sorted
Postconditions: The added Integer is in its rightful place
Algorithm:
    for index in Array:
        if inputInteger < item:
            tmp = item
            insert inputInteger at index item
    return Array
```

### Opgave 3
```
FindDuplicate
Goal: Find the duplicate element in an Array
Input: The Array to be checked
Output: The duplicate element
Preconditions:
Postconditions:
Algorithm:
```


